import LoginScreen from "@/components/LoginFrom";
import { StyleSheet } from "react-native";

const index = () => {
  return <LoginScreen />;
};

export default index;

const styles = StyleSheet.create({});
